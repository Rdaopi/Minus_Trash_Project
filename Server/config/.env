# --- Configurazione Generale ---
NODE_ENV=development                   # Ambiente (development/production/test)
PORT=3001                              # Porta del server

# === Database MongoDB ===
DB_URI=mongodb+srv://RiccardoG:UnicornoVolante@minustrashproject.hhp2uxz.mongodb.net/minus_trash_DB?retryWrites=true&w=majority&appName=minusTrashProject`  # URI di connessione MongoDB

# --- Autenticazione ---
JWT_SECRET=il_tuo_super_segreto_jwt    # Chiave segreta per JWT
JWT_EXPIRES_IN=7d                      # Durata token (es: 7d, 2h)
GOOGLE_CLIENT_ID=                      # (Opzionale) Per OAuth Google
GOOGLE_CLIENT_SECRET=                  # (Opzionale) Per OAuth Google

# --- Integrazioni Esterne ---
GOOGLE_MAPS_API_KEY=                   # Per mappe e geolocalizzazione